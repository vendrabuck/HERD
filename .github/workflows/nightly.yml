# Nightly full-stack gate: everything `make everything` runs against a live
# stack that PR CI does not (Selenium e2e and the locust load test), plus a
# second pass of contract + integration without -x so a regression reports the
# full failure set instead of stopping at the first one. workflow_dispatch
# lets a reviewer run this on any branch before merging a frontend-heavy PR.
#
# The first e2e pass (before the LDAP-mode and Postgres-live phases) runs
# against an unseeded stack, so every test gated on an available device skips
# there by design; after "Seed stack for load test" a second pass,
# `make test-e2e-seeded`, re-runs the same suite against the now-seeded stack
# and fails if anything still skips (issue #629).
#
# Also runs the LDAP-mode stack coverage the master/everything gates run
# (issue #572): infra/ldap-test boots alongside the stack, and after e2e the
# stack's auth service is switched to LDAP mode, tests/integration/
# test_ldap_auth.py and test_ldap_sync_admin.py run against it, and the
# Postgres-live LDAP sync suites run against the stack's own Postgres, via
# `make _gate-ldap-stack-tests` / `make _gate-pg-live-tests` (the same
# internal targets master/everything use, see the Makefile's LDAP comment
# block). Unlike those gates, this job never passes -p to `docker compose`
# (`docker compose build` / `up -d` above use the plain default project), so
# COMPOSE_PROJECT_NAME is set explicitly to match: actions/checkout@v7 checks
# this repository out into $GITHUB_WORKSPACE, which the runner sets to
# <work>/<repo-name>/<repo-name>; this repository's GitHub name is "HERD"
# (`gh repo view` on this remote reports name "HERD", nameWithOwner
# "vendrabuck/HERD", independent of the local clone directory's own name), so
# the checkout directory basename is "HERD" and `docker compose`'s default
# project-name derivation (lowercase, invalid characters stripped; "HERD" has
# none) resolves to "herd" -- the same project docker compose itself already
# uses for every other step in this job.
name: Nightly

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: nightly-${{ github.ref }}
  cancel-in-progress: false

jobs:
  full-stack:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Cache uv
        uses: actions/cache@v6
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('uv.lock') }}
          restore-keys: ${{ runner.os }}-uv-

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Create CI .env
        run: |
          cat > .env <<EOF
          POSTGRES_USER=herd
          POSTGRES_PASSWORD=$(openssl rand -hex 16)
          POSTGRES_DB=herd
          AUTH_SECRET_KEY=$(openssl rand -hex 32)
          INTERNAL_API_TOKEN=$(openssl rand -hex 32)
          SUPERADMIN_EMAIL=admin@example.com
          SUPERADMIN_USERNAME=admin
          SUPERADMIN_PASSWORD=admin123!
          EOF

      - name: Build images
        run: docker compose build

      - name: Start LDAP test server
        run: make ldap-up

      - name: Start stack
        run: docker compose up -d

      - name: Wait for stack healthy
        run: make _master-wait-healthy

      - name: Contract tests
        run: make test-contract

      # No -x here (unlike make test-integration): nightly should surface
      # every failure in one run, not stop at the first.
      - name: Integration tests (full failure set)
        run: uv run pytest tests/integration/ -v --timeout=60

      - name: Install Playwright browser deps
        run: uv run playwright install --with-deps chromium

      - name: E2E tests (Selenium + Playwright)
        run: make test-e2e

      - name: LDAP-mode stack tests
        run: make _gate-ldap-stack-tests COMPOSE_PROJECT_NAME=herd

      - name: Postgres-live LDAP sync tests
        run: make _gate-pg-live-tests

      - name: Stop LDAP test server
        if: always()
        run: make ldap-down

      - name: Seed stack for load test
        run: make seed

      - name: E2E tests (seeded, no skips allowed)
        run: make test-e2e-seeded

      - name: Load test (locust, headless)
        run: HERD_BASE_URL=https://localhost make test-load

      # Issue #648: same container-state-plus-logs split as the CI
      # integration job, so a nightly failure gets the same diagnostics.
      - name: Collect stack diagnostics
        if: failure()
        run: |
          docker compose ps > compose-ps.txt
          docker ps -a > docker-ps-a.txt
          docker compose logs --no-color --timestamps > compose-logs.txt

      - name: Upload failure artifacts
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: nightly-failure-artifacts
          path: |
            compose-ps.txt
            docker-ps-a.txt
            compose-logs.txt
          retention-days: 7
